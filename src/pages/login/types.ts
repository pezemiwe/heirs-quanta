export interface Persona {
  name: string;
  role: string;
  avatar: string;
  email: string;
  access: string;
}

export interface SimplePersona {
  name: string;
  role: string;
  avatar: string;
}
